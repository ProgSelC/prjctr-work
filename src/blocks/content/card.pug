mixin card(card)
    +b.card-block.card.justify-content-between.align-items-center.h-100
        +e.IMG.pic.card-img-top.mt-5(src=`assets/img/${card.src}`, alt=card.alt)
        +e.text-area.card-body.align-middle.flex-grow-0
            +e.P.caption.card-text.text-center= card.text